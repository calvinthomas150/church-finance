{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; if echo \"$f\" | grep -qE '\\.kts?$'; then ./gradlew ktlintFormat -q; fi; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Running ktlint format..."
          }
        ]
      }
    ]
  }
}